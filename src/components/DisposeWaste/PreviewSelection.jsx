// This component is used to display all the waste disposal options the user has selected. It will show the state, waste category, waste type, and collection method. It will import all the selected options from the other components and display them according to the user's selection. The user can also edit their selection from this component.

import React from 'react'

const PreviewSelection = () => {
  return (
    <div>
      
    </div>
  )
}

export default PreviewSelection
